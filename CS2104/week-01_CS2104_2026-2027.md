# CS2104 — Week 01 (2026–2027)

## Exercise 1

The first lecture started with the DSB jingles #1 and #2 at [toglyde.dk](https://toglyde.dk/toglyde/stationslyde-inkl-hoejttalerudkald/).

Using the [fixed-do solfège](https://en.wikipedia.org/wiki/Solf%C3%A8ge#Fixed_do_solf%C3%A8ge), this three-note jingle D/Eb/Bb (pronounced D/S/B in Danish, i.e., D Sharp B) is the acronym of DSB, the Danish Railroad Company "Danske Statsbaner", and is played in Danish rail stations when a train arrives. 
It is due to the Danish composer [Niels Viggo Bentzon](https://en.wikipedia.org/wiki/Niels_Viggo_Bentzon).

This jingle illustrates self-reference since reading aloud yields the name DSB.

Other examples of self-reference include the [liar paradox](https://en.wikipedia.org/wiki/Liar_paradox) and René Magritte's painting ["The Treachery of Images"](https://en.wikipedia.org/wiki/Ceci_n%27est_pas_une_pipe).

**1.a.** Identify the self-reference in the cover of blues guitarist Coco Montoya's album ["The Writing on the Wall"](https://cocomontoya.bandcamp.com/album/writing-on-the-wall).

**1.b.** Identify the self-reference in the cover of Gail Carson Levine's book ["Writing Magic: Creating Stories that Fly"](https://www.goodreads.com/book/show/136218.Writing_Magic).

**1.c.** Identify the self-reference in [Alan Perlis](http://en.wikipedia.org/wiki/Alan_Perlis)'s [programming epigram #59](http://www.cs.yale.edu/homes/perlis-alan/quotes.html):

> In English, every word can be verbed.

All the self-references above are direct. Some self-references are indirect in that something named A refers to something named B that refers to A. For example, for any natural number n, the number n+1 is even if the number n is odd, and the number n+1 is odd if the number n is even.

**1.d.** Identify the indirect self-reference in the following story:

> Two women walk into a bar and talk about the [Bechdel-Wallace test](https://en.wikipedia.org/wiki/Bechdel_test).

**1.e.** Identify the self-reference, if there is one, in the following sentences:

- **1.e.1.** This sentence no verb.
- **1.e.2.** Nostalgia isn't what it used to be.
- **1.e.3.** Fashion is what goes out of fashion. — [Coco Chanel](https://en.wikipedia.org/wiki/Coco_Chanel)
- **1.e.4.** To be or not to be. ([phrase](https://en.wikipedia.org/wiki/To_be_or_not_to_be) · [1942 film](https://en.wikipedia.org/wiki/To_Be_or_Not_to_Be_\(1942_film\)))

### Answers

**1.a.** The album cover features a literal writing on the wall.
**1.b.** The person on the cover is writing the word "Magic", taking the phrase "Writing Magic" literally.
**1.c.** The word "verb" is used as a verb in the sentence.
**1.d.** The Bechdel-Wallace test checks if 2 female characters in a story converse about something other than a male character. This story about two women conversing about the Bechdel-Wallace test itself passes the Bechdel-Wallace test. 
**1.e.1** None.
**1.e.2** The subject is nostalgic for the concept of nostalgia.
**1.e.3** Fashion is ever-changing, so all fashion can eventually become irrelevant ("goes out of fashion").
**1.e.4** None.

---

## Exercise 2

Can you think of

- a book involving a book,
- a theater play involving a theater play, or
- a movie involving a movie?

More generally, can you think of one of these mediums (book, theater play, movie, TV series, etc.) involving one of these mediums?

If so, briefly describe a couple of them and their involvement.

**Subsidiary question:** Give an example where self-reference is used in Computer Science.

### Partial solution for Exercise 2

["A Midsummer Night's Dream"](https://en.wikipedia.org/wiki/A_Midsummer_Night%27s_Dream) qualifies because in Scene 1 of Act V, [Pyramus and Thisbe](https://en.wikipedia.org/wiki/Pyramus_and_Thisbe#Adaptations) is a play within a play.

### Answers

* In Ridge Racer 7, the playable mini-game that appears during loading screens is Galaxian, the classic Namco arcade shooter.
* In computer science, recursive functions are functions that call themselves as part of their execution.

---

## Exercise 3 (a multiple-choice question)

Facetiously, you are asked whether there exists a word in English that contains all of the letters "a", "e", "i", "o", "u", and "y", in that order. Does there exist such a word in English?

Possible answers:

- **a.** Yes, unquestionably. Constant vigilance!
- **b.** No.
- **c.** I cannot possibly know this, and furthermore this facetious question is nefariously out of scope in CS2104.

### Answers

* a. "Adventitiously".

---

## Exercise 4

In reference to LLMs, please view the short piece "The Sorcerer's Apprentice" in ["Fantasia"](https://en.wikipedia.org/wiki/Fantasia_\(1940_film\)).

**N.B.:** While you are at it, expand your musical culture with Paul Dukas's score, as a continuation of Johann Strauss II's waltz that you so magnificently rendered at the beginning of the first lecture ([The Blue Danube](https://en.wikipedia.org/wiki/The_Blue_Danube)).

### Answers

* What?

---

## Exercise 5 — implementing the Sigma notation using a for loop

See [Capital-sigma notation](https://en.wikipedia.org/wiki/Capital-sigma_notation).

The Sigma notation reads, given a non-negative integer n and a function f from (non-negative) integers to integers,

```
sum for i = 0 to n of f(i)
```

and it stands for

```
f(0) + f(1) + ... + f(n)
```

In this notation, 0 is the lower bound, n is the upper bound, and f(i) is the body.

Here, the notation is extended to negative integers in that given a negative integer m and a function f,

```
sum for i = 0 to m of f(i)
```

stands for

```
0
```

The starting point of this exercise is to implement — in the programming language of your choice — a routine / function / procedure / method / what-have-you named `Sigma0` that takes two parameters: an integer n and a function from integer to integer f. Applying `Sigma0` to n and f should yield the result of `f(0) + f(1) + ... + f(n)` when `n` is non-negative, and `0` otherwise.

**5.a.** Devise meaningful tests for your implementation. For example, `f` could be a constant function, or the identity function, or the function that maps an integer `i` to the `i` +first odd natural number (namely `i.2*i + 1` using the lambda notation; then what is computed is `1 + 3 + 5 + ...`).

**5.b.** Implement `Sigma0` using a for loop.

**5.c.** Check that your implementation passes your unit tests. (The testing should be automatic, and since what goes well should go without saying, it should only report the tests that failed.)

**5.d.** Implement a function named `Sigma1` that takes two parameters: an integer n and a function from integer to integer `f`. Applying `Sigma1` to `n` and `f` should yield the result of `f(1) + ... + f(n)` when `n` is positive, and `0` otherwise.

### Answers

```cpp
#include <cmath>
#include <functional>
#include <iostream>
#include <print>
#include <unordered_map>

int Sigma0(int n, std::function<int(int)>& f) {
    if (n < 0)
        return 0;
    int acc = 0;
    for (int i = 0; i <= n; i++) {
        acc += f(i);
    }
    return acc;
}

int Sigma1(int n, std::function<int(int)>& f) {
    if (n < 0)
        return 0;
    int acc = 0;
    for (int i = 1; i <= n; i++) {
        acc += f(i);
    }
    return acc;
}
  
int q5tests() {
    std::vector<int> n = {-3, -2, -1, 0, 1, 2};
    std::vector<std::function<int(int)>> f = {
        [](int i) -> int { return std::pow(i, 3); },
        [](int i) -> int { return 2 * i + 1; }, [](int i) -> int { return 1; },
        [](int i) -> int { return 5; }};
    std::vector<int> expected;
    expected.resize(n.size() * f.size());
    std::fill(expected.begin(), expected.end(), 0);
    expected[12] = 0;
    expected[13] = 1;
    expected[14] = 1;
    expected[15] = 5;
    expected[16] = 1;
    expected[17] = 4;
    expected[18] = 2;
    expected[19] = 10;
    expected[20] = 9;
    expected[21] = 9;
    expected[22] = 3;
    expected[23] = 15;
    for (int i = 0; i < expected.size(); i++) {
        int nIdx = i / f.size();
        int fIdx = i % f.size();
        int res = Sigma0(n[nIdx], f[fIdx]);
        if (res == expected[i]) {
            std::cout << std::format("Test {} passed\n", i);
        } else {
            std::cout << std::format("Test {} expected {}, not {}\n", i,
                                     expected[i], res);
        }
    }
}

int main() {
    q5tests();
}
```

---

## Exercise 6

**6.a.** Given a non-negative integer n, what is the result of

```
sum for i = 0 to 2 * n of floor(n/2)
```

where floor(5/2) = 2 and floor(6/2) = 3.

**6.b.** Given a non-negative integer n, what is the result of

```
sum for i = 0 to 3 * n of floor(n/3)
```

**6.c.** Given a non-negative integer n, what is the result of

```
sum for i = 0 to 4 * n of floor(n/4)
```

Hint: Use the [Online Encyclopedia of Integer Sequences](https://oeis.org/).

**6.d.** Given a non-negative integer n, what is the result of

```
sum for i = 0 to 1 * n of floor(n/1)
```

(Can you simplify this formulation?)

### Answers

For each part let $r$ be the result.

**6.a.** $r = (2n + 1)\left\lfloor  \dfrac{n}{2}  \right\rfloor$
**6.b.** $r = (3n + 1)\left\lfloor  \dfrac{n}{3}  \right\rfloor$
**6.c.** $r = (4n + 1)\left\lfloor  \dfrac{n}{4}  \right\rfloor$
**6.d.** $r = (n + 1)(n)$

---

## Exercise 7

**7.a.** What is the result of

```
1
```

**7.b.** Given a non-negative integer x, what is the result of

```
sum for i1 = 0 to x of
1
```

**7.c.** Given a non-negative integer x, what is the result of

```
sum for i2 = 0 to x of
sum for i1 = 0 to i2 of
1
```

**7.d.** Given a non-negative integer x, what is the result of

```
sum for i3 = 0 to x of
sum for i2 = 0 to i3 of
sum for i1 = 0 to i2 of
1
```

**7.e.** Given a non-negative integer x, what is the result of

```
sum for i4 = 0 to x of
sum for i3 = 0 to i4 of
sum for i2 = 0 to i3 of
sum for i1 = 0 to i2 of
1
```

**7.f.** Implement a function that maps two non-negative integers N and x to

```
sum for iN = 0 to x of
sum for iN-1 = 0 to iN of
...
sum for i2 = 0 to i3 of
sum for i1 = 0 to i2 of
1
```

$\displaystyle\sum_{i_{N}=0}^{x}\sum_{i_{N-1}=0}^{i_{N}}\dots\sum_{i_{2}=0}^{i_{3}}\sum_{i_{1}=0}^{i_{2}}1$

and empirically characterize its result. If possible (ha, ha, ha), justify your characterization.

**N.B.:** Since there is no ambiguity, feel free to write

```
sum for i = 0 to x of
sum for i = 0 to i of
...
sum for i = 0 to i of
sum for i = 0 to i of
1
```

but then you need to say that there are N occurrences of "sum" since this information is no longer present in the notation.

### Answers

For each part let $r$ be the result.

**7.a.** $r = 1$
**7.b.** $r = x + 1$
**7.c.** $r = \dfrac{(x + 1)(x + 2)}{2}$
**7.d.** $r = \dfrac{(x + 1)(x + 2)(x + 3)}{6}$
**7.e.** $r = \dfrac{(x + 1)(x + 2)(x + 3)(x + 4)}{24}$
**7.f.**

```cpp
int q7(int N, int x) {
    if (N < 0)
        return 0;
    int numerator = 1;
    int divisor = 1;
    for (int i = 1; i <= N; i++) {
        numerator *= (x + i);
        divisor *= i;
    }
    return numerator / divisor;
}
```

```
N = 0, x = 0 | 1
N = 0, x = 1 | 1
N = 0, x = 2 | 1
N = 0, x = 3 | 1
N = 0, x = 4 | 1
N = 0, x = 5 | 1
N = 1, x = 0 | 1
N = 1, x = 1 | 2
N = 1, x = 2 | 3
N = 1, x = 3 | 4
N = 1, x = 4 | 5
N = 1, x = 5 | 6
N = 2, x = 0 | 1
N = 2, x = 1 | 3
N = 2, x = 2 | 6
N = 2, x = 3 | 10
N = 2, x = 4 | 15
N = 2, x = 5 | 21
N = 3, x = 0 | 1
N = 3, x = 1 | 4
N = 3, x = 2 | 10
N = 3, x = 3 | 20
N = 3, x = 4 | 35
N = 3, x = 5 | 56
N = 4, x = 0 | 1
N = 4, x = 1 | 5
N = 4, x = 2 | 15
N = 4, x = 3 | 35
N = 4, x = 4 | 70
N = 4, x = 5 | 126
N = 5, x = 0 | 1
N = 5, x = 1 | 6
N = 5, x = 2 | 21
N = 5, x = 3 | 56
N = 5, x = 4 | 126
N = 5, x = 5 | 252
```

These are the outputs for the function for $N \in [0,5]$ and $x \in [0,5]$. I have manually calculated the results and verified them to be accurate. 

By searching the OEIS, these seem to be the binomial results of $\displaystyle\binom{x + N}{N}$.

It can be justified by observing that $\displaystyle\binom{a}{b} = \dfrac{a \cdot (a - 1) \cdot \dots \cdot (a - b + 1)}{k \cdot (k - 1) \cdot \dots \cdot 1}$, so all that's needed is to offset the $x$ by $+N$  in the top part.

---

## Exercise 8

Implement a function that maps non-negative integer N to

```
sum for iN = 0 to 0 of
sum for iN-1 = 0 to iN+1 of
...
sum for i2 = 0 to i3+1 of
sum for i1 = 0 to i2+1 of
1
```

$\displaystyle\sum_{i_{N}=0}^{0}\sum_{i_{N-1}=0}^{i_{N}+1}\dots\sum_{i_{2}=0}^{i_{3}+1}\sum_{i_{1}=0}^{i_{2}+1}1$

and empirically characterize its result. If possible (ha, ha, ha), justify your characterization.

### Answers

```cpp
int q8(int N, int bound, int layer) {
    if (N < 0)
        return 0;
    if (N == 0)
        return 1;
    if (layer == 0)
        return 1;
    int acc = 0;
    for (int i = 0; i <= bound; i++) {
        acc += q8(N, i + 1, layer - 1);
    }
    return acc;
}
```

```
N = 0 | 1
N = 1 | 1
N = 2 | 2
N = 3 | 5
N = 4 | 14
N = 5 | 42
N = 6 | 132
N = 7 | 429
N = 8 | 1430
N = 9 | 4862
N = 10 | 16796
```

These are the outputs for the function for $N \in [0,10]$. I have manually calculated the results and verified them to be accurate. 

By searching the OEIS, these seem to be the Catalan sequence.

After reading the Wikipedia page on Catalan numbers, I have concluded that I do not understand it, and therefore will not attempt to justify the characterization.

---

## Exercise 9

Implement a function that maps non-negative integer N

```
sum for iN = 0 to 0 of
sum for iN-1 = 0 to 1-iN of
...
sum for i2 = 0 to 1-i3 of
sum for i1 = 0 to 1-i2 of
1
```

$\displaystyle\sum_{i_{N}=0}^{0}\sum_{i_{N-1}=0}^{1-i_{N}}\dots\sum_{i_{2}=0}^{1-i_{3}}\sum_{i_{1}=0}^{1-i_{2}}1$

and characterize its result. (Remember that if the upper bound of a summation is negative, the result is 0.) If possible (ha, ha, ha), justify your characterization.

### Answers

```cpp
int q9(int N) {
    if (N < 0)
        return 0;
    if (N <= 1)
        return 1;
    return q9(N - 2) + q9(N - 1);
}
```

```
N = 0 | 1
N = 1 | 1
N = 2 | 2
N = 3 | 3
N = 4 | 5
N = 5 | 8
N = 6 | 13
N = 7 | 21
N = 8 | 34
N = 9 | 55
N = 10 | 89
```

These are the outputs for the function for $N \in [0,10]$. I have manually calculated the results and verified them to be accurate. 

By searching the OEIS, these seem to be the $N$-th number of the fibonacci sequence.

It can be justified by observing that to calculate for $N$, the count of the 2nd sigma operation is between 0 and 1. When that count is 0, it recursively calculates for $N-1$, and when the count is 1, it recursively calculates for $N-2$. Then the 2nd sigma operation simply sums up the result. 

---

## Exercise 10

Implement a function that maps non-negative integer N

```
sum for i = 0 to N of
sum for i = 0 to N-1 of
...
sum for i = 0 to 2 of
sum for i = 0 to 1 of
sum for i = 0 to 0 of
1
```

$\displaystyle\sum_{i=0}^{N}\sum_{i=0}^{N-1}\dots\sum_{i=0}^{2}\sum_{i=0}^{1}\sum_{i=0}^{0}1$

and characterize its result. Justify your characterization.

### Answers

```cpp
int q10(int N) {
    if (N < 0)
        return 0;
    if (N == 0)
        return 1;
    int acc = 1;
    for (int i = N + 1; i >= 1; i--) {
        acc *= i;
    }
    return acc;
}
```

```
N = 0 | 1
N = 1 | 2
N = 2 | 6
N = 3 | 24
N = 4 | 120
N = 5 | 720
N = 6 | 5040
N = 7 | 40320
N = 8 | 362880
N = 9 | 3628800
N = 10 | 39916800
```

These are the outputs for the function for $N \in [0,10]$. I have manually calculated the results and verified them to be accurate. 

By searching the OEIS, these seem to be the $N + 1$ factorials.

It can be justified by observing that each Sigma operation nests another Sigma operation within that decreases the upper bound by 1, from $N$ to 0. Since it's all just adding up 1s, the final sum can be calculated by finding the number of total rounds by all Sigma operations. And the multiplied terms are just the factorial operation $(N + 1)!$ in reverse. 

---

## Exercise 11

Implement a function that maps non-negative integer N

```
sum for i = 0 to 0 of
sum for i = 0 to 1 of
...
sum for i = 0 to N-1 of
sum for i = 0 to N of
1
```

$\displaystyle\sum_{i=0}^{0}\sum_{i=0}^{1}\dots\sum_{i=0}^{N-1}\sum_{i=0}^{N}1$

and characterize its result. Justify your characterization.

### Answers

```cpp
int q11(int N) {
    if (N < 0)
        return 0;
    if (N == 0)
        return 1;
    int acc = 1;
    for (int i = N + 1; i >= 1; i--) {
        acc *= i;
    }
    return acc;
}
```

```
N = 0 | 1
N = 1 | 2
N = 2 | 6
N = 3 | 24
N = 4 | 120
N = 5 | 720
N = 6 | 5040
N = 7 | 40320
N = 8 | 362880
N = 9 | 3628800
N = 10 | 39916800
```

These are the outputs for the function for $N \in [0,10]$. I have manually calculated the results and verified them to be accurate. 

By searching the OEIS, these seem to be the $N + 1$ factorials.

It can be justified by observing that each Sigma operation nests another Sigma operation within that increases the upper bound by 1, from 0 up to $N$. Since it's all just adding up 1s, the final sum can be calculated by finding the number of total rounds by all Sigma operations. And the multiplied terms are just the factorial operation $(N + 1)!$ in reverse. 

---

## Food for thought

These nested sums are not efficient. (Why?) How could their efficiency be improved?

These nested sums, for a certain $N > 0$ requires the $N - 1$ levels of the nested sum to be computed before it can continue. The efficiency can be improved by finding a formula that allows for direct computation.